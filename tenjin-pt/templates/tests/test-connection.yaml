apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tenjin-pt.fullname" . }}-test-connection"
  labels:
    {{- include "tenjin-pt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tenjin-pt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
